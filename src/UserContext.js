import React, { createContext } from 'react';

// Provider & Consumer:
// - Provider: provides data that we're going to be using
// - Consumer: consumes the data you are providing

export const UserContext = createContext();